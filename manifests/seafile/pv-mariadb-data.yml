# Pas pris en compte
apiVersion: v1
kind: PersistentVolume
metadata:
  name: mariadb-data
  namespace: seafile
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /opt/seafile-mysql/db
