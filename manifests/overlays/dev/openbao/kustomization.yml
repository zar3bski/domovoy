apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generatorOptions:
  disableNameSuffixHash: true

secretGenerator:
  - name: openbao-secrets
    namespace: openbao
    envs:
      - secret.env

resources:
  - ../../../base/openbao
