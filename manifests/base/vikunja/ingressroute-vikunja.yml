apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: vikunja
  namespace: vikunja
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: <VIKUNJA_MATCH>
      observability:
        accessLogs: true
        metrics: true
        tracing: true
      services:
        - name: vikunja
          namespace: vikunja
          port: 80
  tls:
    secretName: domain-tls
    domains:
      - main: <VIKUNJA_DOMAIN>
