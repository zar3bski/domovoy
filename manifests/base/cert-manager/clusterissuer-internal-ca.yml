apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: ca-clusterissuer-internal
  namespace: cert-manager
  labels:
    app.kubernetes.io/part-of: domovoy
spec:
  ca:
    secretName: ca-cluster-internal
